import React from "react";
import "./Spinner.css";
const Spinner = (props) => {
  return (
    <div className="spinner-container">
      <span className="loader">
      </span>
    </div>
  );
};

export default Spinner;
